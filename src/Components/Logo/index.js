import React from 'react';

const Logo = (props) => (

  <img src="/logo_ac.png" width={props.width} height={props.height} alt="Logo Colégio Academia" title="Colégio Academia" />
);

export default Logo;
